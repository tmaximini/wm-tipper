extends ../layout

block content
  h1 Alle Mannschaften
  .teams
    each team in teams
      .team
        a(href='/teams/#{team.slug}')= team.name
  .actions
    if user && user.admin
      a.btn.btn-primary(href='/teams/new')
        | Neues Team anlegen
