extends ../layout

block content
  h1= group.name || 'Gruppen Details'
  .groups-details
    .founder
      | Gründer: #{group.founder.profile.name || group.founder.email}
    .members

  .actions
    a.btn.btn-primary(href='/groups/#{group.slug}/edit')
      | Gruppe editieren
